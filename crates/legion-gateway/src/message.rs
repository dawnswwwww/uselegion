//! WebSocket protocol frame types.
//!
//! Re-exported from `legion-protocol` for backward compatibility during the
//! Host / Protocol split. New code should prefer `legion_protocol::websocket`.

pub use legion_protocol::websocket::*;
